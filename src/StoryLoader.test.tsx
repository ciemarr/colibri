import React from 'react';
import ReactDOM from 'react-dom';

describe('StoryLoader', () => {
  // tslint:disable: no-empty
  xit('loads a story', () => {});
  xit('shows a loading indicator', () => {});
  xit('shows a failure indicator', () => {});
  xit('disables "Read Story" button when url is missing', () => {});
  // tslint:enable: no-empty
});
